{% set page_title = "パスワード変更" %}
{% set page_description = "BRT - パスワード変更フォーム。" %}

{#レイアウト継承#}
{% extends "layout/layout.twig" %}

{# スタイルシートの追加 #}
{% block header_scripts_add %}
{% endblock %}

{# ページ本文 #}
{% block page_content %}

    <div class="jumbotron bg-warning text-center d-none d-sm-block">
        <h2>パスワード変更</h2>
    </div>
    <div class="bg-warning py-3 text-center d-sm-none">
        <h2>パスワード変更</h2>
    </div>


    <div class="h3 text-center">
        <h3>必要事項を入力し、決定ボタンを押してください。</h3>
    </div>
    

    <div id="messageText"aria-live="assertive" role="alert">
        {% if message!=null and message!="" %}
            <div class="alert-danger p-3">{{ message | nl2br}}</div>
	    {% endif %}
    </div>

    <form method="post" autocomplete="off">
        <div class="container py-5">
            <div class="row mb-3">
                <label class="col-12 col-sm-4 col-md-3" for="form_oldPassword">現在のパスワード</label>
                <input class ="col-12 col-sm-8 col-md-9" id="form_oldPassword" type="password" name="oldPassword" required>
            </div>
            <div class="row mb-3">
                <label class="col-12 col-sm-4 col-md-3" for="form_password"> 新しいパスワード</label>
                <input class ="col-12 col-sm-8 col-md-9" id="form_password" type="password" name="password" required>
            </div>
            <div class="row mb-3">
                <label class="col-12 col-sm-4 col-md-3" for="form_confirmPassword"> パスワードの確認</label>
                <input class ="col-12 col-sm-8 col-md-9" id="form_confirmPassword" type="password" name="confirmPassword" required>
            </div>
        </div>
        <div class="text-center">
            <p class="mb-3">※パスワードを変更すると、いったんログアウトされます。また、ログイン用URLも変更されます。</p>
            <input type="submit" class="btn btn-success" value="決定">
        </div>
    </form>

{% endblock %}

{# フッダ以降に追加する内容 #}
{% block footer_scripts_add %}
{% endblock %}
